# ADJAI Environment Configuration Example
# Copy this file to .env and update the values before running the app.

PORT=5000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=adjai_db
DB_USER=postgres
DB_PASSWORD=your_password_here

JWT_SECRET=replace_this_with_strong_secret

PAYMENT_MODE=sandbox
